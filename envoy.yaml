node:
  id: id_1
  cluster: main
admin:
  access_log_path: /tmp/envoy_admin_access.log
  address:
    socket_address:
      address: 127.0.0.1
      port_value: "9901"
dynamic_resources:
  cds_config:
    path: /etc/envoy/cds/cds.yaml
  lds_config:
    path: /etc/envoy/lds/lds.yaml